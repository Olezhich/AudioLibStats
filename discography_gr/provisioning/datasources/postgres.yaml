apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    uid: afhvwixkw91j4a          # Фиксированный UID для привязки в дашборде
    url: db:5432       # Имя сервиса в docker-compose
    database: discography
    user: postgres
    secureJsonData:
      password: "postgres"
    jsonData:
      sslmode: disable
      postgresVersion: 1500
      timescaledb: false
    isDefault: true