apiVersion: 1

providers:
  - name: 'Default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    allowUiUpdates: true     # Позволяет менять дашборд через UI без перезаписи файла
    options:
      path: /etc/grafana/provisioning/dashboards-json